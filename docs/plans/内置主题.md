# 内置主题 实现计划

> 关联 spec: ../specs/内置主题.md
> 全局 gate：每个 Task 结束 `go build ./... && go test ./...` 全绿。

### Task 1: 注册 11 个主题 ThemeConfig
- 改：`internal/tui/theme.go`（新增 6 dark：DraculaTheme/TokyoStormTheme/NordTheme/GruvboxTheme/CatppuccinTheme/OneDarkTheme + 5 light：GruvboxLightTheme/CatppuccinLatteTheme/NordLightTheme/TokyoDayTheme/SolarizedLightTheme；builtInThemes map + ThemeNames()；ResolveTheme 查 map，未命中回退 DarkTheme）
- 映射基准：red(1)→LevelError、yellow(3)→LevelWarn、green(2)→LevelInfo、cyan(6)/blue(4)→Accent/TraceID、purple/magenta(5/13)→Source/TitleBG；dark 用 bright 变体（9/10/11 号），light 用标准色 + 设 Bg/Fg
- 验证：`go test ./internal/tui/` + 新增用例：每个主题名可解析且 20 槽位非空
- [ ] 完成

### Task 2: LevelError 色带按主题派生
- 改：`internal/tui/theme.go`（LevelErrorBg/LevelErrorFg 改为从主题 red 亮度派生：暗主题用 red 加深背景 + 白字，亮主题用 red 亮化背景 + 深字；ThemeConfig 增加 IsLight 刴断或按 Bg 亮度计算）
- 验证：`go test ./internal/tui/` + 单测：dark/dracula/nord-light/solarized-light 四主题下色带前景/背景亮度差断言
- [ ] 完成

### Task 3: 配置文档 + README
- 改：`cmd/root.go`（defaultRulesYAML theme 注释：dark 系一行、light 系一行列出全部 13 个主题名）、`README.md`（主题章节 + 每主题一句特色）
- 验证：`go build ./...` + 人工核对注释
- [ ] 完成

### Task 4: tmux 逐主题截图校验 + 测试包
- 执行：`test_build_cmd` 编译到 ~/Documents/，tmux 循环 11 主题截图（level 色/弹窗/选中/ERROR 色带），对比度不足的调整映射
- 验证：截图人工核对 + `go test ./...` 全绿
- [ ] 完成
