# 源选择器 实现计划

> 关联 spec: ../specs/源选择器.md
> 全局 gate：每个 Task 结束 `go build ./... && go test ./...` 全绿。

### Task 1: SSHSource 数据源
- 改：`internal/stream/ssh.go`（新增：SSHSource、NewSSHSource(host, path, tailLines)、Start 起 `ssh -o BatchMode 兜底 host -- tail -f -n N path` 子进程、stderr → ERROR 行写入流、Label/ Cleanup）、`internal/stream/ssh_test.go`（本地起 fake ssh 脚本测行读取/错误路径）
- 验证：`go test ./internal/stream/`
- [ ] 完成

### Task 2: ~/.ssh/config 主机发现 + rules.yaml ssh_hosts
- 改：`internal/stream/ssh.go` 或新 `sshconfig.go`（ParseSSHConfigHosts() 顶层 Host 条目）、`internal/parser/rules.go`（RulesConfig 加 SSHHosts []string）、`cmd/root.go`（默认 YAML 注释 + loadConfig 读出）、`tui.SetSSHHosts()` 注入
- 验证：单测（样例 ssh config 文本解析 + 去重合并）；`go test ./...`
- [ ] 完成

### Task 3: 源选择器 UI（三 tab 弹窗）
- 改：`internal/tui/sourcepicker.go`（新：sourcePickerMode/sourceTab 状态；K8s 分区 = namespace 输入 + kind 分组候选列表 Space 多选（deploy/sts/pod，kubectl get 拉取）Enter → NewMultiK8sSource；本地/SSH 分区输入框 + 候选）、`internal/tui/app.go`（`o` 键、handleSourcePickerKeys 分发、View 分支）、候选异步查询（kubectl get / 目录 ls / 主机列表）用 tea.Cmd + msg 回填
- 验证：单测（tab 切换、Space 勾选、Enter 生成 MultiK8sSource/SSHSource/FileSource、候选渲染）；`go build ./...`
- [ ] 完成

### Task 4: ReplaceStream 源热切换
- 改：`internal/tui/app.go`（ReplaceStream：cancelFunc/Cleanup 旧流、新流 Start、buffer/filteredView/统计/书签清空、streamLabel 更新）、`internal/tui/app_test.go`（mock 流切换用例）
- 验证：`go test ./internal/tui/` + tmux 实测本地文件 ↔ 文件切换
- [ ] 完成

### Task 5: README + 配置注释 + 测试包
- 改：`README.md`（o 键说明、ssh_hosts 配置、SSH 用法示例）、`cmd/root.go`（默认 YAML ssh_hosts 注释）
- 执行：`test_build_cmd` 出包 ~/Documents/，tmux 走查三 tab + 切换 + SSH 错误提示路径
- 验证：`go test ./...` 全绿 + 人工走查
- [ ] 完成
