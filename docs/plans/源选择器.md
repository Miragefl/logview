# 源选择器 v2（浏览式） 实现计划

> 关联 spec: ../specs/源选择器.md
> 全局 gate：每个 Task 结束 `go build ./... && go test ./...` 全绿。

### Task 1: k8s context/ns 浏览
- 改：`internal/tui/sourcepicker.go`（fetchK8sContextsCmd：`kubectl config get-contexts -o name`；fetchK8sNamespacesCmd：`kubectl get ns -o name`）、`sourcepicker_ui.go`（K8s tab 状态机：level 0=context 列表当前项高亮、level 1=ns 列表、level 2=资源多选（现有）；Enter 下钻 / Backspace 返回；context 选中执行 `kubectl config use-context` + 重新拉 ns）
- 验证：单测（状态机 level 切换、context 选中调用、ns 过滤）+ tmux 走查三级浏览
- [ ] 完成

### Task 2: 本地 tab 目录浏览器
- 改：`internal/tui/sourcepicker.go`（loadLocalCandidates 重写：返回目录+日志文件混合列表，目录 / 后缀；输入含通配/绝对路径仍支持）、`sourcepicker_ui.go`（Enter=目录进/文件开；Backspace=上级；`~` 展开；面包屑显示当前路径）
- 验证：单测（目录列表排序、Enter 分派、返回上级）+ tmux 走查 / → /var/log → 文件
- [ ] 完成

### Task 3: SSH tab 远程目录浏览
- 改：`internal/stream/ssh.go`（新增 SSHListDir(host, path)：`ssh -o BatchMode host ls -1 -F path`，返回目录/文件分类）、`internal/tui/sourcepicker.go`（fetchSSHDirCmd 异步）、`sourcepicker_ui.go`（SSH tab 状态机：level 0=主机列表（现过滤逻辑保留）、level 1=远程目录浏览（默认 / 或 /var/log），Enter 进目录/选文件，Backspace 返回主机层或上级目录）
- 验证：单测（SSHListDir 解析 + fake ssh）+ tmux 走查真实主机浏览
- [ ] 完成

### Task 4: README 更新 + 测试包
- 改：`README.md`（o 键三 tab 浏览交互说明）
- 执行：`test_build_cmd` 出包 ~/Documents/，tmux 完整走查：k8s 三级、本地目录、ssh 主机→目录
- [ ] 完成
