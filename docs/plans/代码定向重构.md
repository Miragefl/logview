# 代码定向重构 实现计划

> 关联 spec: ../specs/代码定向重构.md
> 全局 gate：每个 Phase 结束跑 `go build ./... && go test ./...` 全绿后才进下一 Phase。

### Task 1.1: ring buffer 除零 panic
- 改：`internal/buffer/ring.go`（NewRingBuffer 对 capacity<1 取默认值）、`cmd/root.go`（--buffer-size 下限校验）
- 验证：`go test ./internal/buffer` + 新增 capacity=0 用例
- [x] 完成

### Task 1.2: tail Stat 吞错空指针
- 改：`internal/stream/tail.go:89-90`（Stat 失败走 Seek(0,2) 分支）
- 验证：`go test ./internal/stream`
- [x] 完成

### Task 1.3: 末行无换行符丢行
- 改：`internal/stream/filesource.go:50-53`、`internal/stream/tail.go:76-108`（err!=nil 时先消费非空 line 再 break）
- 验证：`go test ./internal/stream` + 新增"文件末行无 \n"用例
- [x] 完成

### Task 1.4: 静默吞错上报（5 处）
- 改：`cmd/root.go:342`（LoadRules 坏 YAML → stderr 警告）、`internal/tui/app.go:1362`（导出失败写入 ExportState 显示 ✗）、`cmd/root.go` k8sCmd 入口（ParseK8sResource 前置校验，替代 `stream/k8s.go:49` 吞错）、`internal/stream/filesource.go:36`（打不开 → 写入一条错误行到 channel）、`internal/tui/app.go:1538`（clipboard 失败不显示"已复制"）
- 验证：`go test ./...` + k8s 非法资源参数报错用例
- [x] 完成

### Task 2.1: 删 SearchIndex 死设施
- 改：删 `internal/buffer/index.go`、`internal/buffer/index_test.go`；`internal/tui/app.go:320,357` 去掉 searchIdx.Add
- 验证：`go build ./... && go test ./...`
- [x] 完成

### Task 2.2: 删热重载死链路
- 改：`internal/tui/app.go`（SetReloadFunc/configReloadMsg/configWatcher 及 shutdown 残留）、`internal/parser/rules.go:164-189`（WatchRules）；`go mod tidy` 移除 fsnotify
- 验证：`go build ./... && go test ./...`，grep 确认无 SetReloadFunc/WatchRules 残留
- [x] 完成

### Task 2.3: 删死样式/死函数/死配置宣传
- 改：`internal/tui/theme.go:95-96`、`internal/tui/style.go`（ErrorLineBg/WarnLineBg/SearchStyle/FieldSeparator/selectedAnsiRestore）、`internal/tui/app.go:1475`（yankWord）、`cmd/root.go:423-431`（默认 YAML 删 error_line_bg/warn_line_bg 注释）、`internal/model/line.go`（删 Level/ParseLevel 及其测试）
- 验证：`go build ./... && go test ./...`
- [x] 完成

### Task 3.1: 抽 textInput 值类型
- 改：新建 `internal/tui/textinput.go`（~20 行：insert/backspace/delete/home/end）；改 `app.go` 三 handler（913-1269）+ confirmSearchTab + 三 popup 光标渲染
- 验证：`go test ./internal/tui`（搜索/高亮/隐藏输入回归）
- [x] 完成

### Task 3.2: 抽 overlayToVL
- 改：`internal/tui/popup.go` 加 overlayToVL；改 helppopup/panel/export_dlg/highlightpopup/hidepopup 5 处调用
- 验证：`go test ./internal/tui`（含 search_popup_overlay_test）
- [x] 完成

### Task 3.3: 渲染层去重
- 改：`internal/tui/logview.go`（renderLineText/renderLineTextWithBg 合并、highlightText/highlightTextWithStyle 合并、行号前缀 3 处抽函数）
- 验证：`go test ./internal/tui`
- [x] 完成

### Task 3.4: ansiRe 单源 + processBatch 复用 processLine
- 改：`internal/model/`（新增 ansi.go 定义 ansiRe）、`internal/parser/regex.go:10`、`internal/tui/app.go:294` 改引 model；`app.go:296-367` processBatch 循环调 processLine
- 验证：`go build ./... && go test ./...`
- [x] 完成

### Task 4.1: loadParsers → Config struct + runTUI
- 改：`cmd/root.go`（Config struct 承载 8 返回值；抽 runTUI(src, resume) 消掉三段复制粘贴）
- 验证：`go build ./... && go test ./...`，手工冒烟 `go run . <file>` / tail / file 三路径
- [x] 完成

### Task 4.2: 全局可变态入 App struct
- 改：`internal/tui/app.go:24,198`（cancelFunc/streamCh 移入 App）
- 验证：`go test ./internal/tui`
- [x] 完成

### Task 4.3: CompileRules 去 panic
- 改：`internal/parser/rules.go`（增 CompileRules 返回 error）、`cmd/root.go:385`（坏正则报 `rule "xxx": invalid regex` 退出）
- 验证：`go test ./internal/parser` + 坏正则退出用例
- [x] 完成

### Task 4.4: cmd 补测试
- 改：新建 `cmd/root_test.go`（expandTailArgs、loadParsers 坏 YAML/缺省路径）
- 验证：`go test ./cmd`
- [x] 完成

### Task 4.5: AutoDetect 分支测试 + fallback 显式标记
- 改：`internal/parser/rules_test.go`（pending/升级/fallback 三分支）、`internal/parser/rules.go:106-141`（RuleConfig 加 fallback 标记替代 `Name()=="plain-text"` 魔法字符串）
- 验证：`go test ./internal/parser`
- [x] 完成

### Task 5.1: 拆搜索族子 model
- 改：新建 `internal/tui/search_model.go`（searchModel：input/cursor/history/字段建议）；`internal/tui/app.go` 对应字段与 handler 迁移
- 验证：`go test ./internal/tui` 全量
- [x] 完成

### Task 5.2: handleNormalKeys 按功能域拆分
- 改：`internal/tui/app.go:651-889`（拆为 navigate/searchToggle/exportToggle/panelToggle 等私有函数）
- 验证：`go test ./internal/tui` 全量
- [x] 完成

### Task 5.3（可选）: View 拆分
- 改：`internal/tui/app.go:1376-1459`
- 验证：`go test ./internal/tui`
- [x] 完成
